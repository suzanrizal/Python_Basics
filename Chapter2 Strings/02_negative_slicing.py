# Convert negative index into positive
name = "sujan"

print(name[1:4])

print(name[:4])  # It is same as print(name[0:5]) If empty in front its 0 
print(name[1:])  # It is same as print(name[1:5]) If empty in last its total length
print(name[1:5])